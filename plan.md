# AI Study Blog 설계

## 목표

AI 스터디의 학습 기록과 발표 자료를 한곳에서 공개·관리할 수 있는 개인 정적 블로그를 만든다. 글은 Markdown/MDX로 작성하고, 발표 자료는 같은 저장소에서 Marp 기반 HTML·PDF로 재사용할 수 있게 한다.

## 범위

### 포함

- Astro 기반 정적 사이트와 MDX 콘텐츠 컬렉션
- 홈, 학습 노트 목록·상세, 발표 목록·상세, 소개 페이지
- 태그·발행일·읽는 시간 등 학습 기록용 메타데이터
- 발표 글에서 브라우저용 슬라이드와 PDF 자료로 이동하는 구조
- `LDH1103.github.io/ai-study-blog` GitHub Pages 자동 배포
- 모바일·데스크톱 반응형과 기본 접근성

### 제외

- 로그인, 댓글, 관리자 화면, DB, CMS
- 외부 AI API 호출·개인화 추천·검색엔진
- 커스텀 도메인과 방문 통계
- 발표 영상 녹화·업로드

## 정보 구조와 디자인

콘텐츠 중심의 개인 연구 노트로 구성한다. 첫 화면은 최근 학습 주제와 다음 발표를 바로 보여주며, 목록은 카드 남발 대신 날짜·태그·요약을 읽기 쉬운 에디토리얼 리스트로 보인다.

```text
Home
├─ Notes       학습 기록 목록과 글
├─ Talks       발표 개요와 슬라이드/PDF 링크
└─ About       스터디 목적과 작성 원칙
```

- 레이아웃: 넓은 여백과 좌측 정렬을 기본으로 한 읽기 중심 구성
- 색상: 종이 같은 밝은 배경, 짙은 잉크색 본문, AI 주제를 구분하는 절제된 파란 액센트
- 타이포그래피: 제목은 강한 대비, 본문과 코드 블록은 오래 읽기 편한 밀도
- 모션: 탐색에 필요한 hover·focus 피드백만 적용

## 기술 결정

| 항목 | 선택 | 이유 |
|---|---|---|
| 사이트 | Astro | 정적 콘텐츠와 개인 페이지를 가볍게 함께 운영 가능 |
| 글 | Markdown/MDX + Content Collections | Git에서 학습 노트를 버전 관리하고, 필요 시 코드·도표를 포함 가능 |
| 발표 | Marp Markdown | 발표 원고를 HTML·PDF·PPTX로 재사용 가능 |
| 배포 | GitHub Pages + GitHub Actions | `main` push로 자동 배포, 별도 서버 관리 불필요 |
| 저장소 | 공개 `ai-study-blog` | GitHub Pages 기본 공개 주소와 공개 학습 아카이브 목적에 맞음 |

## 단계별 계획

1. Astro 프로젝트와 Git 저장소를 초기화하고 MDX·Marp·Pages 배포 설정을 만든다.
2. 공통 레이아웃과 홈·노트·발표·소개 페이지를 구현한다.
3. 첫 학습 노트와 첫 발표 템플릿을 추가하고 콘텐츠 메타데이터 흐름을 검증한다.
4. 빌드·반응형·접근성 기본 점검 후 GitHub Pages에 배포한다.

## 리스크와 대응

- GitHub Pages 프로젝트 주소는 저장소 이름을 포함한다. 초기 주소는 `LDH1103.github.io/ai-study-blog`로 고정하고, 커스텀 도메인은 후속 작업으로 둔다.
- 공개 저장소이므로 초안·개인정보·비공개 발표 자료는 넣지 않는다.
- Marp의 PDF/PPTX 내보내기는 로컬 브라우저 런타임이 필요할 수 있으므로, 첫 배포 성공 기준은 슬라이드 HTML과 사이트 빌드로 한정한다.

## 디자인·정보 구조 보강: AI Lab Log 방향

### 목표

현재의 종이·세리프 중심 인상을, **AI를 공부하고 실험·검증하는 개인 기술 블로그**로 전환한다. 장식적인 AI 이미지나 보라색 gradient 대신, 짙은 그래파이트 배경·기술 메타데이터·명확한 글 목록으로 학습 로그의 성격을 드러낸다.

### 포함

- 별도 `발표` 탭·목록·상세 페이지 제거
- 홈을 최신 글이 중심인 블로그 피드로 전환하고, Hero에는 현재 학습 주제·기록 수·상태를 compact하게 배치
- 학습 노트를 `글`로 명명하고, 순번·날짜·태그·요약이 보이는 고밀도 글 목록 제공
- 큰 감성 문구 대신 "공부 → 실험 → 검증"이라는 짧은 기술 블로그 선언 사용
- sans-serif 제목과 monospace 메타데이터를 분리해 코드·실험 기록의 인상을 강화
- 공통 header, section label, link hover/focus, 본문 여백의 계층 강화
- 발표가 필요할 때는 개별 글의 선택적 "슬라이드 보기" 링크로만 제공
- 데스크톱 Hero 2열과 모바일 단일 열의 자연스러운 전환
- 짙은 그래파이트·차가운 회색·청록 signal accent를 사용하고, 선·그리드·작은 monospace 라벨로 일관성 강화

### 제외

- 새 콘텐츠 종류, 데이터 저장, 애니메이션 중심 연출, 이미지 생성, 외부 폰트·UI 라이브러리 추가
- 발표를 독립적으로 관리·탐색하는 UI

### 영향 범위

- `src/content.config.ts`: `notes` 중심 컬렉션과 글 안의 선택적 슬라이드 메타데이터 정리
- `src/content/notes/`, `src/pages/notes/`: 블로그 글 중심 콘텐츠와 경로
- `src/pages/talks/`, `src/content/talks/`: 제거
- `src/pages/index.astro`, `src/layouts/BaseLayout.astro`, `src/styles/global.css`: 블로그형 홈·탐색·목록·반응형
- `talks/`: 개별 글의 슬라이드 링크가 가리킬 수 있는 Marp 원고만 유지

### 구현 순서

1. 별도 발표 탐색 구조를 제거하고, 기존 발표 자료는 첫 글의 선택적 링크로 연결한다.
2. 기존 노트 컬렉션을 블로그 글로 보이도록 명명·메타데이터·홈 피드를 정리한다.
3. 공통 레이아웃과 홈을 AI Lab Log 정보 계층으로 수정하고 목록·상세 규칙을 맞춘다.
4. 데스크톱 1280px와 모바일 390px에서 실제 렌더링·키보드 focus·내부 링크를 검증한다.

### 성공 기준

- 홈 첫 화면에서 사이트 목적, 현재 관심 주제, 최신 글이 동시에 구분된다.
- 첫 화면은 전형적인 SaaS Hero·그라데이션·장식 카드 없이도 AI 학습 로그로 인식된다.
- 390px에서 메뉴, Hero, 메타데이터, 긴 제목이 겹치거나 잘리지 않는다.
- `/talks/` 탐색은 사라지고, 기존 킥오프 발표 슬라이드는 첫 글에서 정상적으로 열린다.
- Pages base path, 접근성 landmark/focus가 유지된다.

## 디자인 단순화: 단일 탭 블로그

### 목표

상단 탐색을 `글` 하나로 한정하고, AI Lab Log의 상태 카드·순번·과도한 라벨을 걷어낸다. 읽을 글을 바로 고르는 데 집중하는 차분한 개인 AI 스터디 블로그로 정리한다.

### 범위

#### 포함

- 로고는 홈으로만 연결하고, 상단 메뉴는 `글` 1개만 유지
- 홈에서 상태 카드·영문 섹션 라벨·글 순번을 제거하고, 짧은 소개와 최신 글 목록만 제공
- 밝은 중립 배경, 진한 텍스트, 단일 파란색 링크 포인트로 CSS 토큰과 레이아웃을 단순화
- 글 목록·상세의 날짜·제목·요약을 우선하고, 장식성 테두리와 메타데이터 밀도를 낮춤
- 데스크톱 1280px와 모바일 390px에서 한 줄 메뉴·제목·목록의 가독성 및 링크 경로 확인

#### 제외

- 콘텐츠 모델, 글 URL, Marp 슬라이드 링크, GitHub Pages 설정 변경
- 새 UI 라이브러리·외부 폰트·이미지·클라이언트 JavaScript 추가
- 소개 페이지 삭제 또는 글 본문 콘텐츠 수정

### 정보 구조

```text
로고 (홈 링크)                         글

짧은 블로그 소개
최신 글 목록: 날짜 / 제목 / 요약
```

`소개`는 기존 URL을 유지하되 상단 탭에서는 제외한다. 글 하나뿐인 현재 상태에서도 목록 UI가 과장되지 않도록 카드·카운터·상태 패널을 사용하지 않는다.

### 기술 결정

| 항목 | 선택 | 이유 |
|---|---|---|
| 탐색 | 로고 + `글` 1개 | 홈 이동은 브랜드 링크로 보장하면서 메뉴 잡음을 줄임 |
| 홈 | 소개문 + 최신 글 목록 | 학습 현황을 추정하는 장식보다 실제 기록을 우선 |
| 시각 언어 | 밝은 중립 바탕 + 파란 링크 포인트 | 이전 종이·세리프와 최근 다크 대시보드 모두 피하면서 오래 읽기 쉬움 |
| 데이터 | 기존 `notes` 메타 재사용 | 콘텐츠·경로·빌드 계약 변경 없이 화면만 단순화 |

### 영향 범위

- `src/layouts/BaseLayout.astro`: 단일 메뉴와 간결한 헤더·푸터 문구
- `src/pages/index.astro`: 상태 패널·글 순번 없는 홈 피드
- `src/styles/global.css`: 색상 토큰, 간격, 글 목록, 반응형 규칙 단순화
- `src/pages/about.astro`: 메뉴 활성 상태 계약 정리
- `plan.md`, `context.md`, `todo.md`: 설계와 실행 상태 기록

### 구현 순서

1. 공통 탐색을 `글` 하나로 축소하고 숨겨지는 소개 메뉴의 활성 상태 계약을 정리한다.
2. 홈을 단일 소개 블록과 최신 글 목록으로 재구성한다.
3. 전체 CSS를 밝고 절제된 블로그 규칙으로 교체하고 글 목록·상세와 반응형을 맞춘다.
4. 빌드, 정적 링크, 데스크톱·모바일 시각 및 키보드 smoke를 검증한 뒤 Pages에 배포한다.

### 리스크와 대응

- 소개가 메뉴에서 사라져 발견성이 낮아질 수 있다. URL과 기존 페이지는 유지하며, 향후 필요할 때만 푸터 링크로 재노출한다.
- 밝은 테마 전환 시 코드 블록 대비가 낮아질 수 있다. 본문·링크·코드 블록의 명도 대비를 별도 확인한다.

## 승인 대기: 두 화면 구조로 단순화

### 목표

상단의 `글` 탭을 없애고, 사용자에게 보이는 콘텐츠 흐름을 **글 목록**과 **글 상세** 두 가지로만 정리한다.

### 범위

- 홈(`/`)을 전체 공개 글을 보여주는 canonical 목록 화면으로 확정한다.
- 상단 헤더와 탐색을 모두 제거한다.
- 목록 첫 화면의 제목·소개문도 제거하고, 글 목록부터 바로 시작한다.
- 글 상세(`/notes/[slug]/`)에서는 breadcrumb를 제거하고 제목·메타데이터·본문·선택적 슬라이드 링크만 유지한다.
- 기존 `/notes/`와 `/about/` 주소는 삭제하지 않고 목록 화면으로 이동시켜 기존 링크가 404가 되지 않게 한다.

### 비범위

- 콘텐츠 모델, 글 slug, GitHub Pages base path, 슬라이드 파일은 바꾸지 않는다.
- 외부 의존성, 클라이언트 JavaScript, 새 메뉴·카드·검색 기능은 추가하지 않는다.

### 구현 계획

1. 공통 레이아웃에서 헤더·메뉴·활성 경로 계약과 관련 CSS를 제거한다.
2. 홈의 Hero·최신 5개 제한·"글 전체 보기" 링크를 없애고, 전체 글 목록만 둔다.
3. 상세의 breadcrumb를 제거하고, 기존 목록·소개 경로는 목록 홈으로 이동시킨 뒤 빌드·정적 링크·모바일 레이아웃을 확인한다.

### 리스크와 대응

- 과거 `/notes/`·`/about/` 링크가 깨질 수 있다. 삭제 대신 목록 화면 이동으로 호환성을 보존한다.
- 목록이 길어질 수 있다. 기존 에디토리얼 리스트와 반응형 제목 규칙을 그대로 재사용한다.
